<h1>Audio Files:	<%= File.basename(@photo.photo_file.url).to_s %></h1>
<br />

<%= link_to 'Upload new translation', new_gallery_photo_avatar_path(@photo) %>
<br/>
<!--p>This is Photos (Index Action) page. To view/edit a photo, hover over and click on the thumbnail.<p-->
<%= debug(params) if Rails.env.development? %>